MANAKSETU / SIH26107 -- PROJECT GOALS STATUS
Last updated: 2026-12-09 (Session 5 — SSE CRLF parser fix: live chat responding)

COMPLETED GOALS
===============
[x] Defined the project architecture for a FastAPI + ChromaDB + SentenceTransformers RAG backend.
[x] Created the FastAPI application with root and health endpoints.
[x] Created POST /api/chat with request validation, retrieval, generation, and citation response models.
[x] Added CORS configuration for the Next.js frontend.
[x] Added local ChromaDB persistent-collection support.
[x] Added SentenceTransformers embedding support using BAAI/bge-small-en-v1.5.
[x] Added a retrieval distance gate to refuse answers without relevant indexed context.
[x] Added Groq generation with Ollama fallback logic.
[x] Added a grounded RAG prompt and anti-hallucination rules.
[x] Added PDF text extraction and initial processed-text ingestion utilities.
[x] Added a sample local PDF extraction script.
[x] Added Windows PowerShell backend setup, run, and troubleshooting documentation.
[x] Added data-ingestion governance for official BIS sources.
[x] Added .gitignore rules for secrets, Python artifacts, raw/processed data, and Chroma runtime data.
[x] Added sanitized .env.example configuration placeholders.
[x] Fixed collection name mismatch (bis_chunks -> bis_standards) and rebuilt ChromaDB index (6,112 vectors).
[x] Fixed CWD-dependent paths in config.py (CHROMA_PERSIST_DIR resolves against repo root).
[x] Fixed embed model mismatch between ingest/retrieve (unified to BAAI/bge-small-en-v1.5).
[x] Fixed frontend contract (res.response, grounded, Citation type with standard_number, clause, snippet).
[x] Created interactive chat_cli.py for terminal-based RAG testing.
[x] Created validate_rag.py validation harness (5 tests, all passing).
[x] Created Windows PowerShell scripts (run_backend.ps1, ingest_data.ps1, setup_backend_windows.ps1).
[x] Updated setup scripts to auto-build ChromaDB index.
[x] Updated README.md with correct paths and instructions.
[x] Ollama installed and llama3.1:8b model being downloaded for offline fallback.
[x] Added comprehensive /health endpoint with Chroma, embedding model, and Ollama status.
[x] Added unit tests for retrieval gating, citation metadata, and endpoint health (20 tests, all passing).

GOALS TO BE COMPLETED
======================
[x] Configure .env with real GROQ_API_KEY (or verify Ollama works as primary).
[x] Created a gitignored .env file and configured the Groq primary LLM credential; Ollama fallback settings are present and require a local Ollama installation to activate.
[x] Confirmed Python 3.13 compatibility, installed backend dependencies in backend/venv using pre-built packages, and froze exact versions in backend/requirements.txt.
[x] Added Groq LLM integration with openai/gpt-oss-120b model, multi-turn conversation history, CPU throttling guard, and polished chat UI with citations, evidence badges, copy/retry, persistence, and health status pills.
[ ] Add approved BIS source PDFs and text documents to data/raw/.
[ ] Extend ingestion to process every supported .pdf, .txt, and .md source, not just the first PDF.
[ ] Make the ingestion command extract text and build/update Chroma in one reproducible workflow.
[ ] Add source metadata during ingestion: title, source URL, document type, IS number, clause, and ingestion date.
[ ] Implement heading- and clause-aware chunking using the documented 1000-character size and 200-character overlap.
[ ] Build and validate the local Chroma collection (bis_chunks) from the approved corpus.
[ ] Tune retrieval thresholds using a small set of known BIS questions and expected citations.
[x] Implement SSE token streaming for POST /api/chat (now /api/chat/stream and /api/v1/chat/stream; per-token message events + terminal done event with citations/grounded/source/gated).
[x] Update the frontend API client to consume the final SSE event contract (streamChatQuestion; token-by-token rendering with citations/grounded/source UI preserved).
[~] Add Groq timeout/retry behavior and verify automatic fallback to local Ollama.
    - DONE: streaming retry (GROQ_STREAM_RETRIES/GROQ_RETRY_BACKOFF_SECONDS) — transient 429/5xx/drop retried before the first token only; mid-stream failures surface partial text; empty completions fall back. Fixed a latent httpx.Timeout partial-spec bug that would crash the Ollama fallback. 7 hermetic provider tests (tests/test_stream_providers.py).
    - DONE (frontend): Stop button + AbortController signal, rAF token batching, partial-text preserved on mid-stream failure.
    - DONE (frontend, Session 5): fixed the live "chat not responding" bug — sse-starlette frames SSE events with CRLF (`\r\n\r\n`), but the client split only on `\n\n`, so the full body was treated as one block, JSON.parse failed, and no token ever rendered. streamChatQuestion now normalizes `\r\n`→`\n`/`\r`→`\n` in the reader loop, handleSSEBlock strips trailing `\r`, and a guard surfaces "empty response" as an error when the stream ends without a `done`/`error` event. Also made the empty assistant bubble visible: animated 3-dot typing indicator while streaming, "No response received." fallback after a genuinely empty completion, and fixed the pre-existing react-hooks/set-state-in-effect lint error (deferred localStorage restore by a tick).
    - VALIDATED (Session 5): tsc ✓, ESLint 0 errors ✓, `next build` ✓, backend pytest 31/31 ✓, and the fixed parser replayed a captured live CRLF SSE body into 14 tokens + done event (source=groq, joined text == done.response).
    - PENDING: `ollama pull llama3.1:8b` + live fallback verification (offline demo).
[ ] Add an end-to-end integration test: ingest a sample document, retrieve it, and call /api/chat.
[ ] Perform an offline demo test with Groq/network disabled and Ollama available locally.
[ ] Pre-download the embedding model and Ollama model before the demo event.
[ ] Keep INTAKE_SOURCES.txt updated with the exact approved documents and their retrieval dates.

CURRENT PRIORITY ORDER
======================
1. Configure the environment and install backend dependencies. DONE
2. Prepare the approved BIS corpus and build the Chroma index. DONE
3. Validate one grounded /api/chat request with citations. DONE
4. Add health checks and unit tests. DONE
5. Add SSE token streaming (backend stream_answer + /api/chat/stream endpoint + frontend token-by-token rendering + 4 hermetic tests). DONE
6. Verify Groq retry -> Ollama fallback (ollama pull llama3.1:8b) and rehearse the offline demo with network disabled. NEXT
7. End-to-end browser verification and retrieval threshold tuning (RETRIEVAL_MAX_DISTANCE / RETRIEVAL_TOP_K).
